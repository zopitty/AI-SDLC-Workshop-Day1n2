# Environment Configuration for Railway Deployment

# Node Environment (automatically set by Railway)
# NODE_ENV=production

# Port (automatically set by Railway)
# PORT=3000

# JWT Secret (optional - generated automatically if not set)
# JWT_SECRET=your-secure-random-string-here

# Database Configuration
# SQLite is used by default (todos.db)
# For production on Railway, consider using PostgreSQL
# DATABASE_URL=postgresql://user:password@host:port/database

# Application Settings
# TZ=Asia/Singapore
